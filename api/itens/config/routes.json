{
  "routes": [
    {
      "method": "GET",
      "path": "/itens",
      "handler": "itens.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/itens/count",
      "handler": "itens.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/itens/:id",
      "handler": "itens.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/itens",
      "handler": "itens.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/itens/:id",
      "handler": "itens.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/itens/:id",
      "handler": "itens.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
